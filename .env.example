# Example environment file for AlanUI Secure Record Server

# Required: A random string used to salt password hashes for extra security
PASSWORD_SALT=your_random_salt_here

# Required: SHA-256 hash of the master password (never store plain-text passwords)
MASTER_PASSWORD_HASH=your_master_password_hash_here

# Optional: Comma-separated list of SHA-256 hashes for one-time passwords (for temporary access)
ONE_TIME_PASSWORD_HASHES=hash1,hash2,hash3

# Optional: Port for the server to listen on (defaults to 3000 if not set)
PORT=3000

# Optional: Comma-separated list of allowed origins for CORS (e.g., http://localhost:8080,https://your-domain.com)
CORS_ALLOWED_ORIGINS=

# Optional: Base URL for the API (e.g., https://alan.up.railway.app)
API_BASE_URL=

# Optional: Sentry DSN for backend error tracking
SENTRY_DSN=

# Optional: Sentry DSN for frontend error tracking
SENTRY_FRONTEND_DSN=
